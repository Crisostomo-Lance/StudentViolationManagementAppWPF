<Window x:Class="StudentViolationApplication.View.Login_View"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModel="clr-namespace:StudentViolationApplication.ViewModel"
        xmlns:local="clr-namespace:StudentViolationApplication.View" 
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:customcontrols="clr-namespace:StudentViolationApplication.CustomControls"
        mc:Ignorable="d"
        
        Title="Login_View" Height="720" Width="1200"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        MouseDown="Window_MouseDown"
       >
    <Window.DataContext>
        <viewModel:LoginViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>

        <!-- Button Style -->
        <Style x:Key="SignInButton" TargetType="Button">
            <Setter Property="Background" Value="#1E88E5"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                            CornerRadius="6"
                            BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" 
                                        VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1976D2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#1565C0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Label Style -->
        <Style x:Key="FieldLabel" TargetType="Label" >
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Margin" Value="15,10,15,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Width" Value="330"/>
        </Style>

        <!-- ComboBoxItem Style with Icons -->
        <Style x:Key="UserTypeItem" TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Style.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter Property="Background" Value="#E3F2FD"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>


    <Window.Visibility>
        <Binding Path="IsViewVisible" Mode="TwoWay" Converter="{StaticResource BoolToVisibility}"/>
    </Window.Visibility>


    <Border CornerRadius="10"
            Background="White"
            BorderBrush="Black"
            BorderThickness="1"
            Opacity=".95">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="2"/>
                </Grid.ColumnDefinitions>
                <!--#region Control Buttons -->
                <!--Minimize button-->
                <Button x:Name="btnMinimize"
                        Grid.Column="1"
                        Click="btnMinimize_Click">
                    <Button.Content>
                        <materialDesign:PackIcon Kind="WindowMinimize" 
                                                 HorizontalAlignment="Center"
                                                 VerticalAlignment="Center"/>
                    </Button.Content>

                    <Button.Style>
                        <Style  TargetType="Button">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FFC6C6C6"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}">
                                <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <!--Maximize button-->
                <Button x:Name="btnMaximize"
                        Grid.Column="2"
                        Click="btnMaximize_Click"
                        Visibility="Visible">
                    <Button.Content>
                        <materialDesign:PackIcon Kind="WindowMaximize" 
                                                 HorizontalAlignment="Center"
                                                 VerticalAlignment="Center"/>
                    </Button.Content>

                    <Button.Style>
                        <Style  TargetType="Button">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FFC6C6C6"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>

                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}">
                                <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <!--Normal button-->
                <Button x:Name="btnNormal"
                        Grid.Column="2"
                        Click ="btnNormal_Click"
                        Visibility="Collapsed">
                    <Button.Content>
                        <materialDesign:PackIcon Kind="WindowRestore" 
                                                 Width="16" 
                                                 Height="16"
                                                 HorizontalAlignment="Center"
                                                 VerticalAlignment="Center"/>
                    </Button.Content>

                    <Button.Style>
                        <Style  TargetType="Button">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FFC6C6C6"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>

                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}">
                                <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <!--Exit button-->
                <Button x:Name="btnExit"
                        Grid.Column="3"
                        Click="btnExit_Click">

                    <Button.Content>
                        <materialDesign:PackIcon Kind="Close" 
                                                 HorizontalAlignment="Center"
                                                 VerticalAlignment="Center"/>
                    </Button.Content>

                    <Button.Style>
                        <Style  TargetType="Button">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FFE81123"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>

                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" Margin="0,0,9,0">
                                <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <!--#endregion-->

            </Grid>


            <!--#region Non-essential -->
            <StackPanel Grid.Row="1"
                        Width="380"
                        Margin="0,20,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="80"/>
                        <RowDefinition Height="80"/>
                        <RowDefinition Height="450"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <!-- Circular Icon -->
                        <Grid HorizontalAlignment="Center" 
                              VerticalAlignment="Center">
                            <!-- Circle Background -->
                            <Ellipse Width="80" 
                                     Height="80" 
                                     Fill="Blue" 
                                     HorizontalAlignment="Center" 
                                     VerticalAlignment="Center"/>
                            <!-- Icon inside the circle -->
                            <Image Source="/Images/graduation.png"
                                   Stretch="Uniform" 
                                   HorizontalAlignment="Center" 
                                   VerticalAlignment="Center" 
                                   Width="40" Height="40"/>
                        </Grid>
                    </Grid>

                    <Grid Grid.Row="1"
                          Margin="0,10,0,0">

                        <Grid.RowDefinitions>
                            <RowDefinition Height="30"/>
                            <RowDefinition Height="30"/>
                        </Grid.RowDefinitions>

                        <TextBlock  Grid.Row="0"
                                    Text="Student Violation Management"
                                    FontFamily="Segoe UI"
                                    FontSize="22"
                                    FontWeight="DemiBold" 
                                    HorizontalAlignment="Center" 
                                    VerticalAlignment="Center"
                                    />

                        <TextBlock  Grid.Row="1"
                                    Text="College Administration System"
                                    FontFamily="Segoe UI"
                                    FontSize="18"
                                    Foreground="Gray"
                                    HorizontalAlignment="Center"
                                    Margin="0,7,0,0" 
                                    VerticalAlignment="Top"/>
                    </Grid>
                    <!--#endregion-->


                    <Grid Grid.Row="2">
                        <Border CornerRadius="10"
                                Background="White"
                                BorderBrush="DarkGray"
                                BorderThickness="1">

                            <StackPanel Width="380">
                                <TextBlock Text="Sign In"
                                           FontFamily="Segoe UI"
                                           FontSize="20"
                                           FontWeight="SemiBold"
                                           Margin="20,20,20,0"/>

                                <TextBlock Text="Enter your credentials to access the system"
                                           FontSize="16"
                                           FontFamily="Segoe UI"
                                           Margin="20,5,20,0"
                                           Foreground="Gray"
                                           />
                                <TextBlock Text="User Type"
                                           Margin="20,10,20,0"
                                           FontSize="14"
                                           FontFamily="Segoe UI"
                                           FontWeight="SemiBold"
                                           />

                                <ComboBox x:Name="cmbUserType"
                                          Width="330"
                                          Height="45"
                                          Margin="20,10,20,5"
                                          FontSize="16"
                                          FontFamily="Segoe UI"
                                          SelectedIndex="0" 
                                          BorderBrush="White" 
                                          Background="white"
                                          BorderThickness="0" 
                                          Loaded="cmbUserType_Loaded"
                                          >

                                    <ComboBox.ItemContainerStyle>
                                        
                                        <Style TargetType="ComboBoxItem">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="Transparent"/>
                                                    <Setter Property="Foreground" Value="SkyBlue"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>

                                    </ComboBox.ItemContainerStyle>

                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel>
                                                <TextBlock Text="{Binding Name}" FontWeight="Normal" />
                                                <TextBlock Text="{Binding Description}" FontSize="12" Foreground="Blue"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>

                                    <ComboBox.Items>
                                        <ComboBoxItem>
                                            <ComboBoxItem.Content>
                                                <StackPanel>
                                                    <TextBlock Text="Administrator" FontWeight="Bold"/>
                                                    <TextBlock Text="Full system access" FontSize="12" Foreground="Gray"/>
                                                </StackPanel>
                                            </ComboBoxItem.Content>
                                        </ComboBoxItem>

                                        <ComboBoxItem>
                                            <ComboBoxItem.Content>
                                                <StackPanel>
                                                    <TextBlock Text="Staff Member" FontWeight="Bold"/>
                                                    <TextBlock Text="Student affairs access" FontSize="12" Foreground="Gray"/>
                                                </StackPanel>
                                            </ComboBoxItem.Content>
                                        </ComboBoxItem>

                                        <ComboBoxItem>
                                            <ComboBoxItem.Content>
                                                <StackPanel>
                                                    <TextBlock Text="Faculty" FontWeight="Bold"/>
                                                    <TextBlock Text="View-only access" FontSize="12" Foreground="Gray"/>
                                                </StackPanel>
                                            </ComboBoxItem.Content>
                                        </ComboBoxItem>
                                    </ComboBox.Items>
                                </ComboBox>

                                <Label Content="Email" Style="{StaticResource FieldLabel}"/>
                                <Border CornerRadius="6"
                                        BorderBrush="LightGray"
                                        BorderThickness="1"
                                        Width="330"
                                        Height="30"
                                        Margin="15,5,15,0">
                                    <!-- Di gumagana for some reason needed for later debugging -->
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="BorderBrush" Value="LightGray"/>
                                            <Setter Property="BorderThickness" Value="1"/>
                                            <Style.Triggers>
                                                <Trigger Property="IsFocused" Value="True">
                                                    <Setter Property="BorderBrush" Value="SkyBlue"/>
                                                    <Setter Property="BorderThickness" Value="1"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBox x:Name="txtBox_Email"
                                             Text="{Binding UserName}"
                                             Height="auto"
                                             FontSize="14"
                                             FontFamily="Segoe UI"
                                             Background="Transparent"
                                             BorderThickness="0" 
                                             Padding="5"
                                             VerticalAlignment="Center"/>
                                </Border>


                                <Label Content="Password" Style="{StaticResource FieldLabel}" Width="331"/>
                                <Border CornerRadius="6"
                                         BorderBrush="LightGray"
                                         BorderThickness="1"
                                         Height="30"
                                         Width="330">
                                    <customcontrols:BindablePasswordBox Password="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                        Height="auto"
                                                                        FontSize="14"
                                                                       
                                                                        BorderThickness="0" 
                                                                        FontFamily="Segoe UI"               
                                                                        VerticalAlignment="Center">

                                    </customcontrols:BindablePasswordBox>

                                    <!-- Di gumagana for some reason needed for later debugging -->
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="BorderBrush" Value="LightGray"/>
                                            <Setter Property="BorderThickness" Value="1"/>
                                            <Style.Triggers>
                                                <Trigger Property="IsFocused" Value="True">
                                                    <Setter Property="BorderBrush" Value="SkyBlue"/>
                                                    <Setter Property="BorderThickness" Value="1"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                </Border>
                                <TextBlock Text="{Binding ErrorMessage}"
                                           FontSize="12"
                                           TextAlignment="Right"
                                           FontFamily="Segoe UI"
                                           Margin="5"
                                           Foreground="Red" Width="330"
                                           TextWrapping="Wrap"
                                           
                                           />


                                <Button x:Name="btnSignIn"
                                        Content="Sign In"
                                        Width="330"
                                        Margin="15,20,15,0"
                                        Style="{StaticResource SignInButton}"
                                        Command="{Binding LoginCommand}"/>



                            </StackPanel>
                        </Border>

                    </Grid>


                </Grid>



            </StackPanel>

        </Grid>

    </Border>

</Window>